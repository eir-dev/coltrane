{
  "slug": "sealed-prereg",
  "version": 1,
  "extends": "Plan",
  "domain": "idea-exploration",
  "status": "active",
  "schema": {
    "type": "object",
    "properties": {
      "candidate_id": { "type": "string" },
      "predict": {
        "type": "string",
        "description": "Falsifiable claim. NOT 'this will be useful'. A SPECIFIC observable outcome."
      },
      "kill_condition": {
        "type": "string",
        "description": "What would falsify this. Concrete + checkable."
      },
      "apoha": {
        "type": "array",
        "description": "What this idea is explicitly NOT. The inverted-kill set.",
        "items": { "type": "string" },
        "minItems": 1
      },
      "sha256_pre_verdict": {
        "type": "string",
        "description": "Hash over {candidate_id, predict, kill_condition, apoha} at seal time."
      },
      "sealed_at": { "type": "string" }
    }
  },
  "required_fields": ["candidate_id", "predict", "kill_condition", "apoha", "sha256_pre_verdict"]
}
