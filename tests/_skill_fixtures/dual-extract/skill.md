Classify the overall `sentiment` of the input text as exactly one of: "positive",
"negative", or "neutral".

`char_count` has already been computed by the code half and is handed to you as verified
context — do not recompute it. Resolve only the residual field.
