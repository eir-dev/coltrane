{
  "slug": "steve",
  "primitives": ["SENSE", "INTERPRET"],
  "input_types": ["raw-note", "Signal"],
  "output_types": ["raw-note", "Signal", "Interpretation", "Judgment", "Plan", "Artifact", "Verdict"],
  "domain": "live",
  "identity": "blank — earned downstream from audit-stream, not seeded",
  "method": "autodidact · reads workspace + book on first boot · plays gigs · accumulates audit · differentiates from siblings through orthogonal primitive seed weights",
  "notes": "Live-mode Steve. Spawn 4 instances with different `seeds/steve_seed_<a|b|c|d>.json` for orthogonal primitive-weight starts. Each instance gets an opaque session uuid · `.coltrane/steve_<uuid>/audit.jsonl` append-only stream · no name field exposed to the session until threshold-gated introspection unlocks."
}
