{
  "$schema": "https://raw.githubusercontent.com/stryker-mutator/stryker-js/master/packages/api/schema/stryker-core.json",
  "_comment": "Mutation testing config. First run is a subset baseline on src/ledger.ts + src/canonical_form.ts (representative pair: pure-data + canonicalization). Expand mutate[] once the baseline mutation score is recorded. Required devDeps (not yet installed): @stryker-mutator/core, @stryker-mutator/vitest-runner.",
  "packageManager": "npm",
  "testRunner": "vitest",
  "vitest": {
    "configFile": "vitest.config.ts"
  },
  "reporters": ["html", "clear-text", "progress"],
  "coverageAnalysis": "perTest",
  "mutate": [
    "src/ledger.ts",
    "src/canonical_form.ts"
  ],
  "ignorePatterns": [
    "dist",
    "coverage",
    "reports",
    "node_modules",
    "tests/e2e"
  ],
  "timeoutMS": 60000,
  "concurrency": 4,
  "thresholds": {
    "high": 80,
    "low": 60,
    "break": null
  },
  "htmlReporter": {
    "fileName": "reports/mutation/index.html"
  }
}
