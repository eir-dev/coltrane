# Coverage baseline — coltrane-oss

**Run date**: 2026-06-02
**Branch**: tonight/miles/phase-15d-coverage-mutation
**Tooling**: vitest 3.2.4 + @vitest/coverage-v8 ^3.0.0
**Command**: `npx vitest run --coverage`

## Top-line

| Metric | % |
|---|---|
| Statements | 95.94 |
| Branches | 87.70 |
| Functions | 98.64 |
| Lines | 95.94 |

Test surface: 58 files, 492 tests, all passing.

## Below-80% files (the actual finding)

| file | stmts | branches | funcs | lines |
|---|---|---|---|---|
| `genome_writer.ts` | 61.33 | 85.71 | 75 | 61.33 |
| `loader.ts` | 79.80 | 65.00 | 100 | 79.80 |

These two files carry the largest blind spots. `genome_writer.ts` at 61% statements means a third of its code paths are unexercised — likely error branches and edge-case write paths.

## Branch-coverage outliers (≤80%)

`loader.ts` 65%, `tones.ts` 86.66%, `runtime.ts` 86.95%, `server.ts` 86.49%, `ledger.ts` 78.18%, `outputs.ts` 95.55% — most have line-coverage at 100% but conditional branches uncovered.

## Mutation

Stryker config landed (`stryker.conf.json`). Baseline mutation run pending dedicated compute window (~30-60 min wall, since stryker re-runs the suite N times where N = #mutations).

## How to reproduce

```
npm install --legacy-peer-deps  # required: ERESOLVE on @types/node
npx vitest run --coverage
```

Coverage HTML report lands at `./coverage/index.html` after first run.
