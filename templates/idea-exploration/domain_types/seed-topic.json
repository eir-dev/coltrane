{
  "slug": "seed-topic",
  "version": 1,
  "extends": "Signal",
  "domain": "idea-exploration",
  "status": "active",
  "schema": {
    "type": "object",
    "properties": {
      "topic": { "type": "string" },
      "context": { "type": "string" },
      "constraints": {
        "type": "array",
        "items": { "type": "string" }
      }
    }
  },
  "required_fields": ["topic"]
}
