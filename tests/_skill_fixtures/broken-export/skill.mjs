// No default export, no run(). Loading succeeds but there's nothing to call — the
// executor must say so in terms a skill author can act on (mentions run / export).
export const notTheRunFunction = 42;
