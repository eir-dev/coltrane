{
  "slug": "idea-verdict",
  "version": 1,
  "extends": "Verdict",
  "domain": "idea-exploration",
  "status": "active",
  "schema": {
    "type": "object",
    "properties": {
      "candidate_id": { "type": "string" },
      "sha256_pre_verdict": { "type": "string" },
      "verdict": {
        "type": "string",
        "enum": ["RIPENED", "PARTLY-RIPENED", "RIPENED-DIFFERENTLY", "KILL-FIRED", "ABORTED"]
      },
      "evidence": { "type": "string" },
      "what_actually_happened": { "type": "string" }
    }
  },
  "required_fields": ["candidate_id", "sha256_pre_verdict", "verdict"]
}
